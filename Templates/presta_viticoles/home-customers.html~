{% extends "base.html" %}
{% load staticfiles %}
{% block title %} Obtenir Un Devis {% endblock %}

{% block content %}
<div class="row-fluid">
	<table class="table table-striped table-hover spacer">
                <thead>
                    <tr>
                        <th>
                            <a ng-click="triDevis('pwt')">Price With Tax</a>
                            <span class="hSpacer" ng-class="cssChevronsTri('pwt')"></span>
                        </th>
                        <th>
                            <a ng-click="triDevis('pwta')">Price Without Tax</a>
                            <span class="hSpacer" ng-class="cssChevronsTri('pwta')"></span>
                        </th>
                        <th>
                            <a ng-click="triDevis('Company')">Nombre de Plants</a>
                            <span class="hSpacer" ng-class="cssChevronsTri('subject')"></span>
                        </th>
                        <th>
                            <a ng-click="triDevis('Company')">Company</a>
                            <span class="hSpacer" ng-class="cssChevronsTri('Company')"></span>
                        </th>
                        <th>
                            <a ng-click="triDevis('date')">Date</a>
                            <span class="hSpacer" ng-class="cssChevronsTri('date')"></span>
                        </th>
                    </tr>
                </thead>
                <tbody>
					{% for oneestimate in cestimates %}
                    <tr class="clickable">
                        <td>{{oneestimate.price_with_tax}}</td>
                        <td>{{oneestimate.price_without_tax}}</td>
                        <td>{{oneestimate.nb}}</td>
                        <td>{{oneestimate.companyname}}</td>
                        <td>{{oneestimate.creationdate}}</td>
                    </tr>
					{% endfor %}

    	</tbody>
    </table> 
</div>
{% verbatim %} 

{% endverbatim %}
{% endblock %}
